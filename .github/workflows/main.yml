name: 

on:
  schedule:
    - cron: '0 9,21 * * *'
  push:
    branches:
      - 

jobs:
  build:
    runs-on: 

    steps:
      - name: 
        uses: 
    
      - name: 
        uses: 
        with:
          python-version: 

      - name: 
        run: 

      - name: 
        run: {{}}

      #- name: Run cctv
        #run: python ${{ github.workspace }}/cctv.py
        
      #- name: Run weishi
        #run: python ${{ github.workspace }}/weishi.py
        
      #- name: Run qita
        #run: python ${{ github.workspace }}/qita.py
        
      - name: 
        run: |
          git config --local user.email lichao.166@126.com"
          git config --local user.name "ayin166"
          git add .
          git commit *.txt -m "Add generated file"
          git commit *.m3u -m "Add generated file"
          #git pull --rebase
          git push -f
